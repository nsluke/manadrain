{
  "name": "mana-pool-shopping-list",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension to collect MTG cards into a shopping list for Mana Pool",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
